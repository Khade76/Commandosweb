# Production web server
PORT=3000

# BisectHosting Starbase API
# Create the key in Starbase -> Account -> API Credentials.
# Never commit the real key. Configure it directly on the OVH server.
BISECT_API_KEY=
BISECT_PANEL_HOST=https://games.bisecthosting.com

# Canonical Starbase server UUID (GUID format), not the billing/service number.
BISECT_SERVER_UUID=

# Public WARDOGS display settings
WARDOGS_PUBLIC_SERVER_NAME=44th Commando Regiment — WARDOGS Main
WARDOGS_SERVER_REGION=Europe / UK
WARDOGS_MAX_PLAYERS=100
WARDOGS_PUBLIC_JOIN_INFO=
