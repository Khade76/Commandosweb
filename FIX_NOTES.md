# Fix notes

Implemented requested fixes:

- Local assets added under `public/assets` so image references are no longer missing.
- React routed pages added instead of anchor-only tabs.
- Our Servers added for future WARDOGS server display.
- Donate link added for Ko-fi.

The routed entrypoint files are included with `.routed` names for clarity while switching over the existing generated entry files.
