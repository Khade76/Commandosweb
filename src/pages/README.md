# Pages

The routed React page implementation currently lives in `src/App.jsx` so the site can be kept compact while the first version is built out. This folder is reserved for splitting the pages into separate files as the site grows.